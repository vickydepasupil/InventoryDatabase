CREATE DATABASE itemsDB;
USE itemsDB;
DROP TABLE IF EXISTS Employee;
CREATE TABLE items (
	stockID VARCHAR(10) NOT NULL PRIMARY KEY,
	itemName VARCHAR(30) NOT NULL,
	unitPrice DECIMAL(10,2) NOT NULL,
	onStock SMALLINT DEFAULT 0
);